"use client";

/**
 * Client entry for paw paint UI and hooks.
 * Prefer `@/features/paw/client` from app/widgets (RSC-safe barrel split).
 */
export { ClearPaintButton } from "./ui/ClearPaintButton";
export { usePawAnimation } from "./usePawAnimation";

/**
 * Paw feature public API.
 *
 * Client UI/hooks: `@/features/paw/client`.
 *
 * Intentional couplings: `device` (PaintDrawHint mobile copy),
 * `interactive-elements` (paint-aware hint wrapping). Stage 9 may extract shared primitives.
 */
export {};

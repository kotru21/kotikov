/**
 * Re-export of shared `SkillData` for entity consumers.
 * Canonical skills data remains in `@/shared/config/content`.
 */
export type { SkillCategory, SkillData } from "@/shared/config/content";

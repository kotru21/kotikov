/**
 * Re-export of shared `ProjectContent` for entity consumers.
 * Canonical project data remains in `@/shared/config/content` (Stage 9 may co-locate).
 */
export type { ProjectContent as ProjectItem } from "@/shared/config/content";
